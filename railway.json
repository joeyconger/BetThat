{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "npm run migrate && npm run cleanup:cfb && npm run smoke:cfb && npm run verify",
    "restartPolicyType": "NEVER"
  }
}
